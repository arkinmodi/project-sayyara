enum AppointmentTypes {
  SET_APPOINTMENT_STATUS = "SET APPOINTMENT STATUS",
  READ_APPOINTMENTS = "CREATE GET APPOINTMENTS",
  SET_APPOINTMENTS = "SET APPOINTMENT LIST",
}

export default AppointmentTypes;
