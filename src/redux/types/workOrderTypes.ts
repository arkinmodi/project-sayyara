enum WorkOrderTypes {
  GET_WORK_ORDER_BY_ID = "GET WORK ORDER BY ID", // Saga
  PATCH_WORK_ORDER_BY_ID = "PATCH WORK ORDER BY ID", // Saga
  SET_WORK_ORDER = "SET WORK ORDER", // Reducer
}

export default WorkOrderTypes;
